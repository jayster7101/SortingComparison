#include "include.h"
#ifndef SELECTIONSORT_H
#define SELECTIONSORT_H

double selectionsortTimer(std::string [], int);
void selectionsort(std::string [], int); // selection sort algo

int min_pos(std::string [], int, int); // finds minimum position in given sub index of array
std::string lower(std::string ) ; // changes word to lower case;


#endif